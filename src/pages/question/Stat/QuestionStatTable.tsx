import React from 'react'

type PropsTyps = {
  selectComponentType: string
}

// eslint-disable-next-line no-unused-vars
const QuestionStatTable = (props: PropsTyps) => {
  return <div>QusetionStatTable</div>
}

export default QuestionStatTable
